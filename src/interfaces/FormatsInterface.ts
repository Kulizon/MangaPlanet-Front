interface FormatsInterface {
  formats: {
    small: { url: string };
    medium: { url: string };
    large: { url: string };
    thumbnail: { url: string };
  };
}

export default FormatsInterface;
